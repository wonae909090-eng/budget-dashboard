# 이 파일을 secrets.toml로 복사하고 실제 API 키를 넣어주세요.
# secrets.toml은 .gitignore에 등록되어 있어 공개 저장소에 올라가지 않습니다.
# Streamlit Cloud에 배포한 경우: 앱 Settings → Secrets 메뉴에 아래 내용을 붙여넣으면 됩니다.

ANTHROPIC_API_KEY = "sk-ant-..."
